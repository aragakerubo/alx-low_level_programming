#include "main.h"

/**
 * add - Adds two integers and returns the result
 * @i: the first integer
 * @j: the second integer
 *
 * Description: Adds two integers and returns the result
 *
 * Return: Returns the sum of integers i and j
 */
int add(int i, int j)
{
	return (i + j);
}
